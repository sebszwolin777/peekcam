# Background-blur ML stack — the exact versions proven working (see setup_blur.sh).
# Consumed by gen-python-deps.sh, which runs flatpak-pip-generator INSIDE the target
# runtime so pip selects the correct prebuilt manylinux wheels (not source tarballs).
absl-py==2.5.0
cffi==2.1.0
contourpy==1.3.3
cycler==0.12.1
flatbuffers==25.12.19
fonttools==4.63.0
kiwisolver==1.5.0
matplotlib==3.11.1
mediapipe==0.10.35
numpy==2.5.1
opencv-contrib-python==5.0.0.93
packaging==26.2
pycparser==3.0
sounddevice==0.5.5
